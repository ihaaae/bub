#!/usr/bin/env bash
set -euo pipefail

export PATH="$HOME/.local/bin:$PATH"

for executable in uv node pnpm; do
    if ! command -v "$executable" >/dev/null 2>&1; then
        echo "Missing $executable; rerun .agents/setup" >&2
        exit 1
    fi
done

echo "Orb environment ready"
